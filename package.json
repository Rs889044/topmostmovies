{
  "name": "topmostmovies",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Fast, SEO-first Top 10 movie discovery site (Astro + Tailwind v4), monetized with AdSense.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch-data": "node --experimental-strip-types --env-file=.env scripts/fetch-data.ts",
    "validate-seo": "node --experimental-strip-types scripts/validate-seo.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "tailwindcss": "^4.3.1",
    "zod": "^3.24.0"
  }
}
