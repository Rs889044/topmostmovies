# Cached, normalized build-time dataset (movies, lists, taxonomies) lands here as JSON,
# produced by `npm run fetch-data` (scripts/). Committed for reproducible builds.
# See docs/ARCHITECTURE.md and docs/DATA-SOURCES.md.
